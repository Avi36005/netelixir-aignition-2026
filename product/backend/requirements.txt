# ROAScast PRODUCT BACKEND — the online half (FastAPI + LLM).
# run.sh NEVER installs this. Inherits the scored core's pinned deps so the
# product and the grader use the SAME model math.
-r ../../requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

# LLM narrative layer (optional at runtime — degrades to a grounded template
# when no API key is present, so the demo works fully offline).
google-generativeai==0.8.3
groq==0.13.1
